// Bubble sort algorithm in Jack language. @DimitarYordanov17
// (The most lame program I've ever written)


class BubbleSort {
    var int a, b, c, d;
    var bool test;
    
    function void sort(Array array, int length) {
        var int counter, first_variable, second_variable, double_length, main_counter;
    }

    method void sort(asd asd, asd asd) {
        var int pedal, gei, taralqsnik;
    }
}
